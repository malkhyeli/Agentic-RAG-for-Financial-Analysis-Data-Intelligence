# ============================================================================
# STRICT RAG MODE (Document QA)
# ============================================================================
# Required for Strict mode: GroundX API key for PDF indexing/search
# Get yours at: https://dashboard.groundx.ai/
GROUNDX_API_KEY=your_groundx_api_key_here

# ============================================================================
# DEEPRESEARCH MODE (Web Research)
# ============================================================================
# At least ONE of these search provider keys is required for DeepResearch mode.
# Serper is recommended for better Google search results.

# Option 1 (Recommended): Serper API for Google Search
# Get yours at: https://serper.dev/
SERPER_API_KEY=your_serper_api_key_here

# Option 2: LinkUp API for web search
# Get yours at: https://www.linkup.so/
LINKUP_API_KEY=your_linkup_api_key_here

# Search provider selection: "serper", "linkup", or "auto" (default: auto)
# "auto" will try Serper first if SERPER_API_KEY is set, then fall back to LinkUp
RESEARCH_SEARCH_PROVIDER=auto

# ============================================================================
# LLM CONFIGURATION
# ============================================================================
# Used by both modes. Defaults to local Ollama with deepseek-r1:7b
# Ensure Ollama is running: ollama serve
# Pull the model: ollama pull deepseek-r1:7b
DOCQA_LLM_MODEL=ollama/deepseek-r1:7b
OLLAMA_BASE_URL=http://localhost:11434

# ============================================================================
# OPTIONAL: MCP RESEARCH INTEGRATION
# ============================================================================
# Path to the MCP research server (auto-detected by default)
# MCP_RESEARCH_REPO_B_PATH=../Multi-Agent-deep-researcher-mcp-windows-linux

# Debug mode for MCP integration (prints to stderr)
# MCP_DEBUG=0

# URL verification for citations (default: enabled)
# MCP_VERIFY_URLS=1

# ============================================================================
# ERROR MESSAGES
# ============================================================================
# If you see these errors, check the following:
#
# "GROUNDX_API_KEY is not set" -> Add GROUNDX_API_KEY above
# "Missing search API key" -> Add SERPER_API_KEY or LINKUP_API_KEY above (for DeepResearch)
# "Connection refused" at port 11434 -> Start Ollama: ollama serve
# "Model not found" -> Pull the model: ollama pull deepseek-r1:7b
# "Repo B not found" -> Ensure Multi-Agent-deep-researcher-mcp-windows-linux exists
# "server.py not found" -> Check MCP_RESEARCH_REPO_B_PATH setting
